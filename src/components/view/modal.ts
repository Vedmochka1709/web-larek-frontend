/*import { iForm } from "../../types";
import { ensureElement } from "../../utils/utils";
import { Component } from "../base/component"

export class Modal extends Component<iForm> {
    protected formButton: HTMLButtonElement
    protected formContent: HTMLElement
    protected closeButton: HTMLButtonElement;

    constructor(conteiner: HTMLElement) {
        super(conteiner)
        this.formButton = ensureElement('.form_button', this.container)  as HTMLButtonElement; 
        this.formContent = ensureElement('.order', this.container)

        // слушатель
    } 


    // Запоняем контентом
    set contentForm(items:HTMLElement[]) {
        this.formContent.replaceChildren(...items)
    }
}*/


